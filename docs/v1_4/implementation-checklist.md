# v1.4 功能实现清单

## 当前阶段目标

v1.4 重点补齐通知、游客报名、组队大厅空态、组局可见性和活动订票体验，同时继续处理线上预览环境登录后的明显性能问题。

当前主线：

```text
更清楚地收到通知 -> 游客也能先报名 -> 新用户知道下一步做什么 -> 组局可见性和参与关系更明确 -> 需要订票的活动能直接跳转购票
```

## 分支规则

所有功能从 `dev` 拉独立 feature 分支：

```bash
git switch dev
git pull origin dev
git switch -c feature/功能名
```

完成后 PR 到：

```text
feature/* -> dev
```

不要把通知中心、游客报名、性能优化、组队大厅空态和组局参与关系合并到同一个分支里。每个分支只解决一个明确问题，方便测试、回滚和上线验证。

## P0：v1.4 必须优先解决的核心体验

### 1. 通知中心页面优化

建议分支：

```text
feature/notification-center-polish
```

小功能：

- [x] 梳理通知中心的信息层级，区分未读、已读和不同通知类型
- [x] 优化通知列表的空态、加载态和错误态
- [x] 通知卡片展示清晰的触发人、动作、关联活动或组队
- [x] 通知点击后跳转到对应活动、组队、消息或好友请求上下文
- [x] 已读状态和顶部通知数字保持同步
- [x] 移动端通知列表不被导航栏遮挡，长文案不挤压操作区

验收标准：

- 用户能快速判断哪些通知需要处理
- 点击通知后能进入正确上下文
- 已读后通知数字能合理下降
- 通知中心没有长时间空白或无反馈等待

### 2. 游客报名与注册后自动关联

建议分支：

```text
feature/guest-signup-account-linking
```

需求理解：

当前活动或组队报名强依赖登录，会挡住一部分临时用户；微信 WebView 中登录还需要跳转浏览器，流失更明显。v1.4 需要允许用户先用 H5 游客身份报名，之后再通过邮箱或微信号把游客报名记录自动归属到正式账户。这个能力应该让游客能正常参加活动，也让后续注册、登录、补充微信号的用户找回历史报名记录。

小功能：

- [x] 未登录用户可以用游客身份报名活动或组队，不强制跳转登录
- [x] 游客报名表单需要收集名字 / 昵称、电话号码、邮箱、微信号
- [x] 电话、邮箱、微信号至少填写一种联系方式；名字 / 昵称必填
- [x] 表单提示：邮箱或微信号用于之后注册、登录或补充资料时找回报名记录
- [x] 微信 WebView 中优先保留 H5 游客报名路径，避免因为登录跳转浏览器造成流失
- [x] 新增游客报名记录模型，保存活动 / 组队、展示昵称、电话、邮箱、微信号、报名留言、创建时间、来源环境
- [x] 游客报名成功后，在活动参与人区域展示游客的名字 / 昵称
- [x] 游客头像用名字 / 昵称的第一个字符生成，支持中文首字，并用稳定哈希生成不同底色，类似 Google 字母头像
- [x] 用户资料新增微信号字段，允许用户填写和修改
- [x] 用户注册、登录或资料更新后，如果已验证账户邮箱与游客报名邮箱一致，自动绑定对应游客报名记录
- [x] 用户填写或修改微信号后，如果与游客报名微信号一致，自动绑定对应游客报名记录
- [x] 已有账户用户如果曾经用同一邮箱游客报名，登录后也能自动找回并归属报名记录
- [x] 绑定成功后，游客报名记录转为正式用户参与记录，活动详情和个人报名记录都能看到
- [x] 绑定过程需要幂等，重复登录、重复修改微信号不会创建重复参与记录
- [x] 如果游客报名对应活动已经结束、取消或报名状态变化，绑定时需要保留历史状态，不错误重开报名
- [x] 对联系方式做必要脱敏展示，避免在活动详情中公开手机号、邮箱或微信号
- [x] 防止不同用户通过随意填写联系方式错误认领他人记录

验收标准：

- 未登录用户可以在 H5 和微信 WebView 中完成报名提交
- 游客报名后，活动参与人区域能看到游客昵称头像，但不会暴露联系方式
- 注册或登录后，匹配已验证账户邮箱的游客记录能自动归属到正式账户
- 用户填写或修改微信号后，匹配微信号的游客记录能自动归属到正式账户
- 正式用户能在自己的报名记录中看到被自动归属的历史游客报名
- 信息不一致时不会误关联
- 同一游客记录不会被多个用户重复认领
- 重复提交、重复关联和恶意认领都有防护
- 微信 WebView 中不再因为必须登录才能报名而阻断用户

实现注意事项：

- 邮箱自动绑定依赖 Clerk 已验证邮箱，避免只靠未验证输入认领他人报名
- 微信号属于用户手动填写信息，已做大小写、空格和最小长度规范化；如果游客记录同时有邮箱和微信，微信匹配还需要已验证邮箱一致，避免被别人只填微信号抢走
- 电话号码可以作为游客联系方式保存；是否用于自动绑定取决于正式用户资料是否已有可信手机号字段
- 游客记录和正式参与记录之间需要保留可追溯关系，方便排查误绑定
- 自动绑定不应该覆盖用户已有参与状态，也不应该绕过活动容量、审批、取消等业务规则
- 对游客报名接口增加节流、防重复提交和基础垃圾数据防护；当前使用来源 hash 指纹限制短时间内同活动和全站提交频率，不保存原始 IP

### 3. 网页性能优化：登录后预览环境加载慢

建议分支：

```text
feature/preview-auth-performance
```

分析文档：

```text
docs/v1_4/preview-auth-performance-analysis.md
```

当前状态：

- [x] 已完成登录态性能静态分析，明确全局 layout、页面查询、客户端请求和索引层面的优化方向
- [x] 已实现第一阶段低风险优化：全局 layout 并行读取、合并 profile / admin 查询、延迟通知 badge 首屏刷新
- [x] 已实现页面级优化：通用活动列表活动 / 公共活动并行读取，搜索页 primary results 并行读取，组队大厅非默认分区按需加载
- [ ] 待接入 Preview 真实日志，验证冷启动、认证链路和数据库查询的实际耗时
- [ ] 待记录优化前后关键页面耗时对比，并继续拆分页面级优化

小功能：

- [ ] 记录线上预览环境登录前、登录后关键页面首屏加载耗时
- [x] 排查登录后是否触发过多用户态查询、串行查询或全局 layout 查询
- [x] 检查通知、好友、头像菜单、权限判断等全局数据是否拖慢每个页面
- [x] 将可并行的数据读取改为并行读取
- [x] 减少登录态公共布局中不必要的阻塞请求
- [x] 对非强实时数据使用缓存、延迟加载或客户端渐进加载
- [x] 给超过预期的用户态数据加载增加稳定 loading 或 shimmer
- [x] 输出性能结论，明确慢点来自 Preview 冷启动、数据库查询、认证链路还是页面实现

验收标准：

- 登录后页面不应频繁出现超过 10 秒的无反馈等待
- 可以明确定位主要耗时来源
- Preview 环境冷启动时也有明确加载反馈
- 优化后关键页面有前后耗时对比
- `npm run typecheck` 通过

## P1：组队大厅和组局关系体验

### 4. 组队大厅新用户空态

建议分支：

```text
feature/lobby-new-user-empty-state
```

小功能：

- [x] 当用户没有相关组队数据时展示引导空态
- [x] 空态文案覆盖“先加好友”“我来组局”“去发现活动”三个方向
- [x] 空态中的按钮能跳转到添加好友、发起组局和活动发现页
- [x] 筛选条件为 0 条结果时，展示和新用户首次进入不同的空态
- [x] 空态卡片在移动端和桌面端都不挤压筛选区
- [x] 保持与当前组队大厅视觉风格一致

参考文案：

```text
你的组队大厅还空着
公园？逛展？还是看电影？先组个局，去逛逛活动，再加几个好友吧。
```

验收标准：

- 新用户进入组队大厅时知道下一步可以做什么
- 三个引导入口都能正常跳转
- 筛选无结果和真正无数据的状态可区分
- 页面不再只显示空列表或 0 计数

### 5. 开放局 / 私人局显示

建议分支：

```text
feature/open-private-crew-display
```

小功能：

- [ ] 组队大厅列表明确展示开放局和私人局标签
- [ ] 筛选项中的“开放”计数和列表展示保持一致
- [ ] 组队详情页展示当前局的可见范围
- [ ] 私人局只对符合权限的用户显示
- [ ] 好友可见、陌生人不可见的规则需要在列表、详情和搜索入口保持一致
- [ ] 对不可见或已失效的私人局给出合理兜底，不暴露敏感信息

验收标准：

- 用户能从列表直接判断这是开放局还是私人局
- 非好友不能在公开列表看到私人局
- 好友可以正常看到私人局
- 计数、筛选结果和实际列表一致

### 6. 自己发起的组局增加“是否自己参与”按钮

建议分支：

```text
feature/organizer-self-participation-toggle
```

小功能：

- [ ] 发起组局时增加“我也参与”开关
- [ ] 编辑自己发起的组局时可以修改是否自己参与
- [ ] 默认值需要符合当前产品预期，并在 UI 中表达清楚
- [ ] 参与人数、剩余名额和报名列表需要正确计算发起人是否参与
- [ ] 发起人不参与时，不应占用报名名额
- [ ] 发起人参与时，详情页和报名列表需要展示发起人的参与状态
- [ ] 防止发起人重复作为普通报名用户占用两次名额

验收标准：

- 发起人可以明确选择自己是否参与组局
- 名额计算和报名列表与选择状态一致
- 编辑后详情页、列表卡片和筛选计数同步更新
- `npm run typecheck` 通过

## P2：活动信息补齐和展示一致性

### 7. 公共活动详情页增加复制按钮

建议分支：

```text
feature/public-event-copy-address-enrichment
```

小功能：

- [x] `/public-events/[publicEventId]` 活动页面增加复制按钮
- [x] 复制按钮能力和 `/activities/[activityId]` 页面保持一致
- [x] 复制内容包含活动标题、时间、地点、费用和页面链接
- [x] 复制成功和失败都需要有明确反馈
- [x] 移动端按钮不挤压详情页主要操作区

验收标准：

- 用户可以在公共活动详情页一键复制活动信息
- `/public-events/` 和 `/activities/` 的复制体验一致
- `npm run typecheck` 通过

### 8. Paris 泛地址与地图地址回填排查

建议分支：

```text
feature/public-event-copy-address-enrichment
```

小功能：

- [x] 排查线上部分活动地址只显示 `Paris` 但地图已有定位的原因
- [x] 确认数据源中是否存在具体地址、经纬度、场馆名或地图 place 信息
- [x] 如果有经纬度但地址过于笼统，评估是否可以用地图信息回填更具体地址
- [x] 明确哪些地址可以自动替换，哪些需要保留原始来源避免误导
- [x] 对回填结果保留来源标记，避免把推断地址当作官方地址
- [x] 对无法确定具体地址的活动展示合理兜底文案

实现说明：

- Paris Open Data 导入中，如果 `address_name` 和 `address_street` 都缺失，会回退为 `Paris`。
- 本版本不做不可靠的反向地理编码替换；当地址只有 `Paris` 但有经纬度时，列表和详情展示“地图定位可用，具体地址以官方页面为准”。
- 复制地点时会保留来源地址、坐标和地图链接，避免把推断位置展示成官方地址。

验收标准：

- 可以解释为什么部分活动只显示 `Paris`
- 有地图坐标时，能判断是否具备可靠的具体地址来源
- 地址回填不会把不确定位置展示成确定地址
- 公共活动列表和详情页的地点展示规则一致

### 9. 活动订票链接与立即抢票按钮

建议分支：

```text
feature/activity-ticket-link-cta
```

小功能：

- [x] 活动数据支持可选订票链接字段，用于保存官方购票、预约或报名页面地址
- [x] 创建活动和编辑活动时，可以填写订票链接，并对 URL 做基础校验
- [x] 只有填写了订票链接的活动，详情页才展示“立即抢票”按钮
- [x] “立即抢票”按钮点击后跳转到订票链接，外部链接需要新窗口打开并带安全属性
- [x] 订票按钮在移动端和桌面端都放在主要操作区，不挤压“立即报名”或组队信息
- [x] 对已结束、已取消或链接缺失的活动，不展示误导性的抢票入口
- [x] 点击抢票按钮需要记录基础 analytics，方便后续判断转化
- [x] 公共活动和用户发起的组局需要明确区分：公共活动可跳官方订票，组局报名仍走站内报名
- [x] Open Data 后续导入时，如果源数据提供 `access_link` / `access_link_text`，写入订票链接和按钮文案字段
- [x] 从公共活动发起的组局详情页，在主要操作区提供返回对应活动详情页的入口
- [x] 活动介绍文本里的 URL 自动变成可点击外链，并提供一键复制按钮

验收标准：

- 运营或用户能为需要订票的活动配置订票链接
- 有订票链接的活动详情页能清楚看到“立即抢票”
- 点击按钮能稳定跳转到正确订票地址
- 没有订票链接的活动不出现空按钮或无效按钮
- 移动端首屏主要操作不被新增按钮挤压
- 后续 Open Data 导入的订票或预约链接能进入结构化字段，详情页可直接展示
- 从活动创建的组队详情页能直接进入对应公共活动详情页
- 活动介绍中的官方链接或其他 URL 不需要用户手动选中文本复制
- `npm run typecheck` 通过

### 10. 旧活动订票链接回填

建议分支：

```text
feature/backfill-ticket-links
```

目标：

对已经入库的 Open Data 活动进行一次性回填，把历史 `sourcePayload` 里的订票或预约链接补到新增的结构化字段里，避免只有后续导入的新活动才有“立即抢票”。

小功能：

- [x] 新增一次性回填脚本，默认 dry-run，只输出会更新的数量和样例
- [x] 支持 `--write` 或明确参数后才真实写库
- [x] 优先回填 `PublicEvent.ticketUrl` / `PublicEvent.ticketLabel`
- [x] 如仍存在旧 `Activity` 形式的 Open Data 活动，再回填 `Activity.ticketUrl` / `Activity.ticketLabel`
- [x] 从 `sourcePayload.access_link` 读取订票或预约链接
- [x] 从 `sourcePayload.access_link_text` 读取按钮文案，但需要复用订票文案过滤规则
- [x] 回填后输出 created / updated / skipped / invalidUrl 等统计
- [x] 后续 Open Data 导入和回填共用订票按钮文案存储规则，通用文案不写入数据库，前端按 locale 展示默认按钮

注意事项：

- 只处理 `source` 属于 `paris-opendata` / `paris-opendata:que-faire-a-paris` 的历史数据
- 只补 `ticketUrl` 为空的记录，不能覆盖人工编辑过的订票链接
- `access_link` 必须是有效 `http` 或 `https` URL，其他值跳过
- `access_link_text` 如果是 URL、过长、或通用法语文案如 `Réservation`，不要写入 `ticketLabel`
- 回填前先在 preview 数据库 dry-run，并保留样例 title / id / ticketUrl 供人工确认
- 生产环境执行前需要确认数据库备份或可回滚方案
- dry-run 命令：`npm run db:backfill-ticket-links --workspace=apps/web`
- 写入命令：`npm run db:backfill-ticket-links --workspace=apps/web -- --write`

验收标准：

- dry-run 能明确显示将回填多少条、跳过多少条、无效链接多少条
- `--write` 执行后，历史 Open Data 活动详情页能出现“立即抢票”
- 人工配置过订票链接的活动不会被覆盖
- 回填脚本可重复执行，重复执行不会产生额外副作用

### 11. 组队 / 活动详情分享入口与宣传图优化

建议分支：

```text
feature/share-poster-entry-polish
```

需求理解：

组队详情页已有“分享活动”能力，但组件放在详情页底部，移动端和桌面端都容易被用户忽略。该能力需要保留并前移，但不能只做成一个孤立按钮，否则仍然不明显。应优化为更靠近主要操作区的分享模块：默认展示清晰入口和高频操作，展开后提供复制标题、复制链接、下载宣传图和下载二维码。组队详情页文案改为“组队分享”，公共活动详情页新增同类能力并命名为“活动分享”。

小功能：

- [x] 组队详情页保留复制标题、复制链接、下载宣传图、下载二维码能力
- [x] 组队详情页分享模块从底部前移到参与信息 / 报名操作附近，移动端和桌面端都能较早看到
- [x] 组队详情页分享模块标题改为“组队分享”
- [x] 公共活动详情页新增分享模块，标题为“活动分享”
- [x] 分享模块默认展示轻量入口和主要操作，避免占用过多首屏空间
- [x] 展开后能看到完整分享操作，不隐藏现有功能
- [x] 宣传图生成时，如果详情页有封面图，需要把封面图放入宣传图
- [x] 宣传图品牌标识改用 `apps/web/public/logo.png`，不再使用旧的黑色 `NF` 方块 logo
- [x] 移动端分享模块不能挤压报名、抢票、发起组队等主要按钮
- [x] 分享点击、宣传图下载、二维码下载继续保留 analytics 记录

验收标准：

- 用户在组队详情页进入后能较快看到“组队分享”
- 用户在公共活动详情页能看到“活动分享”
- 分享模块不会因为放得太低而被忽略，也不会占据过多首屏
- 下载的宣传图包含活动 / 组队标题、时间、地点、费用、二维码和新 logo
- 有封面图的详情页，生成宣传图时能展示该封面图
- `npm run typecheck` 通过

### 12. 地图跳转 Google Maps

建议分支：

```text
feature/google-maps-deep-link
```

需求理解：

当前活动详情页已有地图展示和打开地图能力，但默认是 OpenStreetMap。用户在移动端或海外使用场景中，经常需要直接跳到 Google Maps 进行路线规划。因此地图模块需要提供 Google Maps 跳转入口，同时保留现有 OpenStreetMap 展示。

小功能：

- [x] 活动 / 公共活动详情页地图模块提供 Google Maps 外链入口
- [x] 有具体地址时优先使用地址和城市构造 Google Maps query，避免 Google 只显示经纬度
- [x] 地址过于笼统或没有地址时，使用 `latitude,longitude` 构造 Google Maps query
- [x] 外链新窗口打开，并带 `target="_blank"` / `rel="noreferrer"`
- [x] 复制活动信息时可以继续保留地图链接或坐标信息

验收标准：

- [x] 点击 Google Maps 能打开对应地点或搜索结果
- [x] 地址只有 `Paris` 且有坐标时，Google Maps 使用坐标而不是泛地址；有具体地址时优先展示地址搜索
- [x] 没有地图数据时不展示无效按钮
- [x] 移动端按钮不挤压报名、抢票、组队等主要操作

### 13. 近七日活动天气组件

建议分支：

```text
feature/activity-weather-widget
```

需求理解：

如果活动发生在近七日内，并且活动时间是某一个具体日期，可以在详情页展示天气组件。天气应基于活动地点，而不是用户当前位置。需要优先复用或抽象一个公共 `météo` / weather 组件，避免在活动页硬编码天气 UI。

小功能：

- [x] 新增公共 `météo` / weather 天气组件，活动详情页和组队详情页共用
- [x] 仅当活动开始时间在未来七日内时展示
- [x] 仅当活动有明确日期时展示；长期展览、多日活动或时间范围过宽时不展示
- [x] 天气查询优先使用活动经纬度；没有经纬度时通过城市 / 地址走 Open-Meteo geocoding 兜底
- [x] 天气组件展示温度、天气状态、降雨概率和风速等出行信息
- [x] 天气数据由客户端异步加载，包含加载态、错误态和无数据兜底，不阻塞详情页首屏

验收标准：

- 近七日单日活动能看到基于活动地点的天气
- 超过七日、缺少地点或长期活动不展示误导性天气
- 天气接口失败时页面不崩溃
- 移动端不会挤压抢票、报名、组队等主要操作

### 14. DeepL Free 活动与评论翻译

建议分支：

```text
feature/deepl-translation-cache
```

需求理解：

接入 DeepL API Free，用于活动内容和评论的多语言翻译。为了保证详情页首屏流畅，并保留用户直接阅读原文的权利，活动和评论都不自动翻译。活动详情页提供“翻译主要信息”按钮，用户点击后翻译标题、描述、地点、费用、行程等主要字段，并把翻译结果写入数据库缓存；下一个用户同语言再次点击时直接读库，不重复消耗 API 配额。评论旁边展示机器人图标和“翻译”文案，用户点击后手动翻译并缓存。已翻译评论需要支持查看 original 原文。

小功能：

- [x] 设计活动翻译缓存表，按 entity、字段名、locale、源内容 hash 存储翻译
- [x] 活动标题、描述、地点、费用、行程等主要字段通过“翻译主要信息”按钮手动翻译
- [x] 活动翻译缓存命中时不调用 DeepL
- [x] 活动源内容变化后通过 hash 失效旧翻译
- [x] 接入 DeepL API Free，密钥通过环境变量配置，服务端调用，不暴露到前端
- [x] 评论旁边展示机器人图标 + 本地化“翻译 / Translate / Traduire”按钮
- [x] 评论翻译点击后才调用 DeepL，并把结果写入数据库
- [x] 已翻译评论支持查看 original 原文 / 查看翻译之间切换
- [x] 翻译接口需要错误提示和失败兜底，不影响评论正常展示

验收标准：

- 用户进入活动详情页时先看到原文，页面不会因为翻译请求拖慢
- 点击活动“翻译主要信息”后，活动主要内容能展示对应语言
- 同一个活动同一个 locale 第二次点击不重复调用 DeepL
- 评论翻译是用户手动触发，不会批量消耗额度
- 已翻译评论可以切换查看原文
- DeepL 配额或接口异常时页面仍可正常浏览原始内容

### 15. 活动列表视图切换：卡片 / 按日期

建议分支：

```text
feature/activity-list-view-toggle
```

需求理解：

活动发现页当前只有卡片视图，适合浏览图片、收藏和进入详情，但用户如果想快速判断“今天、明天、本周有什么活动”，需要按时间重新扫描所有卡片。完整月历视图现在不建议第一版直接做：活动数据里有长期展览、多日活动和组局，直接铺到月历会在移动端非常拥挤，也会增加日期范围查询和多日展示的复杂度。因此第一版建议做“卡片 / 按日期”视图切换，其中按日期视图采用 agenda 列表：按开始日期分组展示活动，多日或长期活动独立归入长期活动分组，避免刷屏。

小功能：

- [x] `/activities` 活动发现页增加视图切换控件：卡片 / 按日期
- [x] 默认继续使用卡片视图，避免改变现有浏览习惯
- [x] 按日期视图按活动开始日期分组，如今天、明天、本周具体日期
- [x] 多日活动、长期展览归入“长期活动”或类似分组，避免在每一天重复出现
- [x] 视图切换需要保留当前筛选条件、分页和排序语义
- [x] 切换状态优先通过 URL query 保留，方便刷新、分享和返回
- [x] 移动端按日期视图使用纵向 agenda，不做横向月历挤压布局
- [x] 卡片视图和按日期视图复用同一批活动数据，避免额外重复请求
- [x] 空态、加载态、错误态在两种视图下都要清晰可用

验收标准：

- 用户可以在活动发现页切换卡片视图和按日期视图
- 用户能快速看出近期每天有哪些活动
- 长期活动不会在日期视图里刷屏
- 切换视图后筛选条件不丢失
- 移动端不出现横向溢出或按钮挤压
- 第一版不强制实现完整月历；后续可在数据和交互稳定后再评估周视图 / 月历视图

### 16. 左滑右滑发现新活动

建议分支：

```text
feature/activity-swipe-discovery
```

需求理解：

在活动发现页现有卡片和按日期视图之外，新增一个更轻量、更娱乐化的“滑动发现”入口。用户像翻活动卡片一样快速浏览公开活动：左滑下一张，右滑上一张，点“查看”进入详情，点“收藏”才写入真实收藏。这个功能主要解决“列表太多、不知道先看哪个”的问题，尤其适合组队大厅移动端碎片化浏览。第一版不替代活动列表，也不把左右滑作为用户兴趣动作；候选活动只展示未开始的公开活动，并在会话内随机排序，不固定展示同一批头部活动。

小功能：

- [x] 组队大厅移动端增加“滑动发现”模块，只展示公开活动，不展示组局卡
- [x] 未登录组队大厅预览也展示滑动发现，公开组局可以先刷后登录
- [x] 游客预览页移除顶部介绍横幅，让首屏直接进入滑动发现和公开组局
- [x] 网页端首页暂不展示大卡片刷动模块，避免占用过多桌面首屏空间；第一版聚焦组队大厅移动端
- [x] 活动卡片展示大图、标题、时间、地点、活动类型和当前状态
- [x] 左滑切到下一张，右滑切到上一张，不把左右滑作为兴趣或收藏动作
- [x] 左右切换不循环，第一张右滑不回到最后一张，最后一张左滑不回到第一张
- [x] 收藏和查看按钮固定放在卡片底部，避免用户误以为右滑就是收藏
- [x] 点击“查看”进入对应活动详情页
- [x] 队列只使用未开始、未取消的公开活动
- [x] 候选活动会话内随机排序，不固定从同一批活动开始
- [x] 不限制候选活动总数，只限制首屏实际渲染和加载的卡片数量
- [x] 不展示“第几个 / 总数”进度数字，避免把浏览入口做得像任务流程
- [x] 顶部说明压缩成“滑一滑”加图标，减少文案占位
- [x] 动画轻量，拖动和释放只在客户端状态内完成
- [x] 支持可访问性：键盘左右键、按钮 aria label
- [x] 支持系统减少动画偏好，减少旋转和释放动画
- [x] 返回详情页后保留当前浏览到的卡片位置
- [x] 真实收藏只由心形按钮控制，左右滑不改变用户数据
- [x] 堆叠预览卡片只作为视觉层，避免隐藏卡片按钮被键盘或读屏误触
- [x] 只让当前顶层卡片加载图片和交互控件，减少首屏不必要资源请求
- [x] 拖动位移使用 `requestAnimationFrame` 节流，降低高频 pointermove 渲染压力
- [x] 移动端去掉外层大容器边框，只保留活动卡片本身，减少层层框线
- [x] 压缩滑动模块高度，让公开组局第一行尽早露出
- [x] 滑动模块放在筛选分类上方，用户先刷活动，再按需精筛组局
- [x] 移动端筛选改成横向 chip rail，减少分类区占用的首屏高度
- [x] 桌面端筛选标签列适配法语等长文案，避免 `CATEGORIE` 遮挡按钮
- [x] 收藏、好友发起、好友参加等延迟分区支持点击优先加载和空闲后台补齐计数
- [x] 记录基础埋点：曝光、进入详情

验收标准：

- 用户能在移动端通过左右滑快速浏览活动
- 不会因为滑动视图影响原有卡片视图和按日期视图
- 桌面首页不因为刷卡模块出现大面积空白或孤立右侧卡片
- 法语、英语、中文筛选标签和按钮不会互相遮挡
- 延迟加载分区不会长时间只显示 `...` 且没有反馈
- 收藏和查看入口始终在卡片底部可见
- 左滑下一张、右滑上一张，不和收藏状态混淆
- 返回详情页后当前卡片位置不丢失
- 键盘焦点只进入当前顶层卡片，不会进入背后的堆叠卡片
- 快速拖动时卡片响应稳定，不因为高频移动事件产生明显卡顿
- 活动队列加载失败时有明确反馈，不出现长时间空白
- 首屏只加载必要卡片，不把大量活动详情或图片一次性预热
