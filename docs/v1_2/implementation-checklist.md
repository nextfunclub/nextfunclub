# v1.2 功能实现清单

## 当前阶段目标

v1.2 仍处于进行中阶段，核心目标是把“活动信息”和“用户组队”拆清楚，同时补齐社交、评论和举报相关的基础能力。

当前主线：

```text
用户发现活动信息 → 基于活动发起组队 → 好友或陌生人加入组队 → 通过评论 / 私聊 / 通知完成活动前沟通
```

## 分支规则

所有功能从 `dev` 拉独立 feature 分支：

```bash
git switch dev
git pull origin dev
git switch -c feature/功能名
```

完成后 PR 到：

```text
feature/* → dev
```

不要把评论、举报、好友入口、私聊等功能合并到同一个分支里。每个分支只解决一个明确问题，方便测试、回滚和团队协作。

## P0：v1.2 必须收口的核心体验

### 1. 活动信息位与组队模型收口

建议分支：

```text
feature/public-event-team-model-plan
```

小功能：

- [x] 区分“活动信息”和“组队”
- [x] 活动信息卡片不展示报名人数和名额状态
- [x] 活动信息页只作为信息详情页，不直接报名
- [x] 用户可以基于活动信息发起组队
- [x] 组队可以被报名、审核、取消报名
- [x] 已有 API / 爬虫导入的旧活动兼容为活动信息
- [x] 用户自发的组队不允许再被复制成另一个组队
- [x] 活动发现页保持原有卡片排布，只调整语义和操作文案

验收标准：

- 活动发现页展示的是活动信息和正在组队的车队
- 活动信息卡片不出现 `0/99`、名额、已报名等人数信息
- 活动信息详情页可以进入“发起组队”流程
- 组队详情页保留报名、审核、取消报名能力
- 旧导入活动在新模型下可以正常展示和跳转

### 2. 好友入口放入头像下拉栏

建议分支：

```text
feature/profile-friend-entry
```

小功能：

- [x] 头像下拉栏展示当前用户好友号
- [x] 头像下拉栏增加“添加好友”入口
- [x] 添加好友入口打开统一弹窗，不再依赖单独说明页
- [x] 删除下拉栏中过长的说明文字
- [x] 保留个人空间、消息、通知、账号设置、退出登录等核心入口
- [x] 移动端和网页端下拉栏文案保持简洁一致

验收标准：

- 用户可以在头像下拉栏第一时间看到自己的好友号
- 用户可以从头像下拉栏直接打开添加好友弹窗
- 下拉栏没有开发视角或过长解释性文字
- 移动端按钮文字不换行，不遮挡页面内容

### 3. 活动发起人私聊入口

建议分支：

```text
feature/organizer-direct-message
```

小功能：

- [x] 活动详情页展示“联系发起人”入口
- [x] 登录用户可以从活动详情页发起私聊
- [x] 自动创建或复用与发起人的点对点会话
- [x] 用户不能和自己发起私聊
- [x] 未登录用户点击后进入登录流程
- [x] 移动端入口放在详情页主要操作区域，避免被底部导航遮挡

验收标准：

- 用户可以从活动详情页直接进入与发起人的聊天
- 已存在会话时不会重复创建
- 自己查看自己发起的活动时不显示无意义的私聊入口
- `npm run typecheck` 通过

## P1：社交搜索、评论和举报基础能力

### 4. 全站好友搜索

建议分支：

```text
feature/global-friend-search
```

小功能：

- [x] 全站搜索支持搜索好友号
- [x] 全站搜索支持搜索用户昵称
- [x] 搜索结果展示用户头像、昵称和好友号
- [x] 搜索结果中可以发起好友申请
- [x] 不暴露 Google 邮箱等非必要个人信息
- [x] 增加空结果、加载中、失败兜底状态
- [x] 移动端搜索结果不遮挡主要页面内容

验收标准：

- 输入好友号可以准确找到用户
- 输入昵称可以找到匹配用户
- 用户可以从搜索结果发起好友申请
- 搜索结果不展示敏感登录信息

### 5. 评论编辑、删除与回复

建议分支：

```text
feature/activity-comment-threading
```

小功能：

- [x] 评论作者可以编辑自己的评论
- [x] 评论作者可以删除自己的评论
- [x] 删除评论采用软删除，保留讨论结构
- [x] 支持对评论进行一级回复
- [x] 活动详情页展示评论和回复
- [x] 评论输入框限制纯文本
- [x] 评论和回复都需要登录后才能发布
- [x] 移动端评论区避免多层卡片嵌套

验收标准：

- 用户可以修改自己的评论
- 用户可以删除自己的评论
- 其他用户不能修改或删除别人的评论
- 用户可以回复已有评论
- 删除父评论后，回复仍能保持可读结构

### 6. 举报系统基础模型与管理员角色持久化

建议分支：

```text
feature/report-system-foundation
```

小功能：

- [x] 新增 `Report` 数据模型
- [x] 支持举报对象类型：用户、活动信息、组队、评论
- [x] 支持举报原因枚举
- [x] 支持举报补充说明
- [x] 记录举报人、被举报对象、处理状态和创建时间
- [x] 同一用户对同一对象的重复举报需要限制
- [x] 为后续后台处理预留查询字段
- [x] 新增 `UserRole`，支持普通用户 / 管理员
- [x] 管理员判断迁移为数据库角色优先
- [x] 保留环境变量和 Clerk metadata 作为紧急兜底

验收标准：

- 举报记录可以写入数据库
- 举报对象类型明确，不用多个临时表分散处理
- 重复举报不会产生大量垃圾数据
- `UserProfile.role = ADMIN` 的用户可以进入管理后台
- 未配置数据库角色时，原有 env / Clerk metadata 管理员仍可作为兜底
- Prisma generate 和 typecheck 通过

### 7. 举报入口与后台处理

建议分支：

```text
feature/report-ui-admin-review
```

小功能：

- [x] 用户主页增加举报入口
- [x] 活动信息页增加举报入口
- [x] 组队详情页增加举报入口
- [x] 评论旁增加举报入口
- [x] 举报弹窗文案简洁，不鼓励滥用
- [x] 管理员后台展示举报列表
- [x] 管理员可以标记待处理、已处理、驳回
- [x] MVP 阶段先不自动隐藏内容，由管理员人工处理

验收标准：

- 登录用户可以提交举报
- 未登录用户点击举报后进入登录流程
- 管理员可以看到举报记录
- 管理员处理举报后状态可更新

## P2：体验打磨与通知联动

### 8. 评论和举报通知

建议分支：

```text
feature/comment-report-notifications
```

小功能：

- [x] 评论被回复时通知原评论作者
- [x] 活动发起人收到新评论通知
- [x] 举报提交后给用户轻量反馈
- [x] 管理员收到新举报提醒
- [x] 通知中心展示对应类型通知

验收标准：

- 用户能知道自己的评论被回复
- 活动发起人能看到活动下的新评论提示
- 管理员不会遗漏新举报

### 9. 社交入口统一体验

建议分支：

```text
feature/social-entry-polish
```

小功能：

- [ ] 统一好友、消息、通知、个人空间的入口文案
- [ ] 移动端底部导航只保留必要入口
- [ ] 桌面端头像下拉栏减少说明文字
- [ ] 空状态文案改为面向用户的自然语言
- [ ] 删除开发视角的解释性文字

验收标准：

- 用户不需要理解技术概念也能完成操作

## P3：数据系统与产品决策

### 10. 全局埋点方案设计

当前文档：

```text
docs/v1_2/global-analytics-tracking-plan.md
```

小功能：

- [x] 明确埋点只服务产品团队、活动发起人和管理员
- [x] 剔除没人使用的冗余事件和开发排错事件
- [x] 定义北极星指标和阶段性指标
- [x] 明确活动发起人可见的数据：浏览、报名、沟通、分享、入口来源
- [x] 明确管理员可见的数据：举报处理、公共活动来源价值
- [x] 定义全站事件命名规范
- [x] 定义通用事件结构
- [x] 规划核心漏斗：活动发现、活动信息转组队、组队报名、活动前沟通、举报处理
- [x] 明确第一版最小事件清单
- [x] 将搜索、筛选、微信 WebView 登录提示和关键业务失败纳入第一版
- [x] 定义事件质量、去重和管理员数据过滤规则
- [x] 明确隐私边界，不采集聊天、评论、举报、留言正文
- [x] 定义上线验收清单
- [x] 拆分后续实施阶段：基础设施、活动级数据、社交分享、管理员运营

验收标准：

- 团队能基于文档拆分后续埋点开发分支
- 埋点方案能服务产品策略，而不是单纯记录访问量
- 活动发起人能基于数据看到自己活动的表现
- 管理员能基于数据处理社区和内容运营问题
- 开发排错事件不混入产品行为埋点
- 后续实现前已明确哪些数据不能采集
- 后续实现前已明确客户端事件和服务端事件的边界
- 后续实现前已明确 Preview / Production 数据隔离和看板过滤规则

### 11. 埋点基础设施

建议分支：

```text
feature/analytics-foundation
```

小功能：

- [x] 新增 `AnalyticsEvent` 数据模型
- [x] 新增事件白名单
- [x] 新增事件字段校验和敏感字段清洗
- [x] 新增服务端埋点 helper
- [x] 新增客户端埋点 helper
- [x] 新增 `/api/analytics/events` 上报接口
- [x] 区分 `development` / `preview` / `production`
- [x] 埋点失败不阻断用户主流程

验收标准：

- 合法事件可以写入数据库
- 未知事件不能写入数据库
- 聊天、评论、举报、报名留言正文不能进入埋点字段
- Preview 和 Production 可以通过 `environment` 区分

### 12. 核心产品和活动级埋点

建议分支：

```text
feature/analytics-activity-insights
```

小功能：

- [x] 活动发现页记录列表曝光
- [x] 活动卡片点击记录来源入口
- [x] 活动信息详情页记录浏览
- [x] 组队详情页记录浏览
- [x] 发起组队入口记录点击
- [x] 组队创建成功记录转化
- [x] 报名入口记录点击意图
- [x] 报名成功记录真实转化
- [x] 活动搜索提交记录结果数，不保存搜索原文
- [x] 活动筛选使用记录筛选维度
- [x] 关键业务表单失败记录原因码
- [x] 微信内置浏览器登录提示页记录出现次数
- [x] 评论、回复、联系发起人和私聊发送记录活动前沟通
- [x] 复制字段、复制链接、下载宣传图记录活动传播动作
- [x] 通知点击记录是否真的进入目标页面
- [x] 举报提交和管理员处理记录运营处理动作
- [x] 公共活动来源链接和转组队记录来源价值
- [x] 活动发起人可以看到浏览、报名、沟通和传播数据概览

验收标准：

- 可以还原活动发现、详情浏览、发起组队、报名成功的核心漏斗
- 可以判断搜索和筛选是否真的帮助用户找到活动
- 可以看到关键业务表单失败的主要阻碍原因
- 活动发起人能看到浏览、报名点击、报名成功、评论沟通、联系发起人、分享复制和转化率
- 埋点不采集搜索原文、报名留言、评论正文、聊天正文

### 13. 沟通、好友和分享埋点

建议分支：

```text
feature/analytics-social-sharing
```

小功能：

- [x] 好友申请发送成功记录 `friend_request_sent`
- [x] 好友申请通过成功记录 `friend_request_accepted`
- [x] 好友事件只记录入口、查找方式和是否有留言，不记录好友号、昵称、邮箱或留言正文
- [x] 私聊打开、消息发送、联系发起人沿用服务端成功埋点
- [x] 评论和回复沿用服务端成功埋点
- [x] 分享工具补充二维码下载入口并记录 `qr_code_shared`
- [x] 链接复制、字段复制、宣传图下载继续归入活动传播数据

验收标准：

- 可以判断好友申请是否真正形成好友关系
- 可以判断活动前沟通是否发生在评论、私聊或联系发起人入口
- 可以给活动发起人统计链接、字段、宣传图和二维码传播动作
- 不采集聊天正文、评论正文、好友搜索原文、好友号、邮箱和留言正文

### 14. 管理员运营数据

建议分支：

```text
feature/admin-analytics-operations
```

小功能：

- [x] 举报提交事件要求带举报实体和来源入口
- [x] 举报处理事件要求带处理状态变化
- [x] 公共活动来源点击按当前环境统计
- [x] 公共活动转组队按当前环境统计
- [x] 后台举报页展示最近 30 天运营概览
- [x] 后台举报页展示待处理举报、平均处理时间、来源点击、转组队数量
- [x] 后台举报页展示举报对象分布
- [x] 新增独立管理员数据后台 `/admin/analytics`
- [x] 数据后台展示活跃用户、有效互动、活动发现、活动信息转组队、报名转化
- [x] 数据后台展示沟通、好友、分享和公共活动来源价值
- [x] 数据后台增加重点判断区，提示管理员优先查看的运营问题
- [x] 数据后台支持 7 天 / 30 天 / 90 天时间范围切换
- [x] 数据后台支持总览、用户路径、来源价值、社区反馈分视图
- [x] 数据后台总览增加使用趋势柱状图
- [x] 数据后台总览增加热门活动排行
- [x] 数据后台增加社区反馈模块，展示举报新增、待处理、已处理和对象分布
- [x] 数据后台将来源入口、公共活动来源和举报处理入口按操作习惯重新分区
- [x] 数据后台减少嵌套卡片，优化网页端和移动端阅读顺序
- [x] 新增数据后台优化分支说明文档
- [x] 管理员头像菜单增加数据后台入口

验收标准：

- 管理员能看到当前举报处理压力
- 管理员能看到最近 30 天举报处理效率
- 管理员能判断公共活动来源是否带来点击和组队
- 管理员能从一个页面快速判断产品核心漏斗是否健康
- 管理员页面不展示技术事件名或开发视角描述
- 埋点失败或统计读取失败不影响举报处理主流程
